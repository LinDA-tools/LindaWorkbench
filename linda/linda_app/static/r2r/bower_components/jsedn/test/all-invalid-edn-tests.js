// Generated by CoffeeScript 1.8.0
(function() {
  var assertion, edn, equals, fs, testDir;

  assertion = require("./assertion");

  fs = require("fs");

  equals = require("equals");

  edn = require("../src/reader");

  testDir = "./edn-tests/invalid-edn";

  fs.readdir(testDir, function(err, files) {
    return files.forEach(function(file) {
      return fs.readFile("" + testDir + "/" + file, "utf-8", function(err, invalidEdn) {
        var e, r;
        try {
          r = edn.parse(invalidEdn);
          return console.log("[FAIL] " + file + " was parsed\n\tPARSED: " + invalidEdn + "\n\tINTO: " + r);
        } catch (_error) {
          e = _error;
          return console.log("[OK] " + invalidEdn + " was NOT parsed - which is a good thing\n\tGOT: " + e);
        }
      });
    });
  });

}).call(this);
